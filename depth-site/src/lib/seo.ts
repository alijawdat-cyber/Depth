// Kept for future use if you re-enable next-seo. Currently unused to avoid
// duplicate React renderer and invalid hook call during App Router rendering.
export const siteUrl = (process.env.NEXT_PUBLIC_SITE_URL || "https://depth-agency.com").replace(/\/$/, "");


